const { GoogleGenAI } = reqire("@google/genai");
const ai = new GoogleGenAI({ apikey: "" })
async function main() {
    const response = await ai.models.genreateContent
}



async function getweather(location) {
    for (const { city })
}